---
- hosts: 
    - centostestvms
    - centosvms
  remote_user: "{{ansible_user}}"
  become: yes
  become_method: sudo
  connection: "ssh"

  tasks:
    - import_role:
        name: "setup-admin-users-groups-logins"