---
apollo_deps:
    - ssl-cert
    - lsb-release
    - curl
    - wget
    - netcat
    - git
    - zip
    - unzip
    - build-essential
    - python3-pip
    - libpq-dev
    - gnupg2
    - xmlstarlet
    - libpng-dev
    - zlib1g-dev
    - libexpat1-dev
    - ant

monitoring_deps:
    - nagios-nrpe-server
    - nagios-plugins

# TODO: prometheus exporters for Grafana
#       wget -q -O - https://s3-eu-west-1.amazonaws.com/deb.robustperception.io/41EFC99D.gpg | sudo apt-key add -
#       sudo apt-get update -y
#       sudo apt-get install -y \
#       prometheus-node-exporter prometheus-postgres-exporter prometheus-pushgateway prometheus-alertmanager
# TODO part 2: then upgrade these packages to 19.10 versions directly with .deb files

